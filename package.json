{
  "name": "@cinatra-ai/wordpress-assistant",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "The Cinatra WordPress authoring assistant: an agent-kind assistant extension whose cinatra/config.json declares the WordPress assistant (persona, authoring skill bundle, remote launch into the connected site's wp-admin, webhook turn delivery). Handles no credentials of its own — every WordPress call resolves through the WordPress assistant connector.",
  "type": "module",
  "files": [
    "cinatra"
  ],
  "cinatra": {
    "apiVersion": "cinatra.ai/v1",
    "kind": "agent",
    "dependencies": [
      {
        "packageName": "@cinatra-ai/wordpress-assistant-connector",
        "edgeType": "runtime",
        "versionConstraint": {
          "kind": "semver-range",
          "range": "^0.1.2"
        },
        "requirement": "required",
        "kind": "connector"
      }
    ]
  }
}
